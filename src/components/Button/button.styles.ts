import styled from '@emotion/styled';

export const StyledButton = styled.button({
  background: 'none',
  border: '2px solid #fff',
  padding: '6px 12px',
  borderRadius: '4px',
  color: '#fff',
  fontWeight: 'bold',
  cursor: 'pointer',
  fontSize: '1em',
  '&:hover': {
    background: '#c23866',
    color: '#fff',
  },
});
